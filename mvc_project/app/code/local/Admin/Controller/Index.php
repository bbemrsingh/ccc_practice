<?php

class Admin_Controller_Index extends Core_Controller_Admin_Action
{

    protected $_allowedAction = [];
    public function indexAction()
    {
        // $layout = $this->getLayout();

        // ->setTemplate('core/admin.phtml');

        // $child = $layout->getChild('left');

        // $header = $layout->createBlock('admin/dashboard');
        // $layout->getChild('content')->addChild('header', $header);
        // $layout->toHtml();
        // 

        // $content = $layout->createBlock('admin/content');
        // $child->addChild('content', $content);

        // $layout->getChild('head')->addCss('admin/main.css');
        // $layout->getChild('head')->addCss('admin/header.css');
        // $layout->getChild('head')->addCss('admin/content.css');}
    }
}